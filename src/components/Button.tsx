const Button = ({ children }) => {
  return <button className="  text-lg   bg-[#49bbe0]    text-white  text-center py-4 px-8 hover:bg-[#87c14e] hover:text-white transition-all ease-in-out   duration-500 cursor-pointer">{children}</button>;
};

export default Button;
